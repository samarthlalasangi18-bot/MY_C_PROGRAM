ude<stdio.h>
int main()
{
    int num;
    printf("enter the number\n");
    scanf("%d",&num);
    if(num >= 10 && num <= 100)
    {
       
        printf("%d is in the range 0-100\n",num);
    }
    else
     {
         printf("%d is not in the range 0-100\n",num);
    }
}
