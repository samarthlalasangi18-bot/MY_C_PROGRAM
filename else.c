#include <stdio.h>
int main() {
    int number;
    printf("enter the number\n");
    scanf("%d",&number);
    if(number>0)
    printf("positive number\n",&number);
    else
    printf("negative number\n",&number);
}
