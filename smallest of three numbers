#include <stdio.h>
int main()
{
   int a,b,c;
   printf("enter the three number\n");
   scanf("%d%d%d",&a,&b,&c);
    if(a<b)
    {
    if(a<c)
    printf("%d is small\n",a);
    else
    printf("%d is small\n",c);
   }
  else if(b<c)
  printf ("%d is small",b);
  else
  printf("%d is small",c);
}
