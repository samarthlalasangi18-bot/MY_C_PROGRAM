#include <stdio.h>
int main(){
    int array[5];
    int sum=0;
    printf("enter the elements");
    for(int i=0;i<5;i++){
        printf("enter the elements\n");
        scanf(" %d",&array[i]);
        sum=sum+array[i];
    }
    printf("sum=%d\n",sum);
}
