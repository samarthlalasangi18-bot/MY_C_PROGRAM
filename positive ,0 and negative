#include <stdio.h>
int main()
{
   int num;
   printf("enter the number\n");
   scanf("%d",&num);
    if(num>=0)
{ 
    if (num>0)
       printf("%d is a positive number\n",num);
    else
    printf("%d is 0\n",num);
   }else
   printf("%d is negative number\n",num);
}
